import React from 'react'
import ReactDOM from 'react-dom/client'

const App = () => <h1>Hello from 500MW Shutdown AI Agent!</h1>

ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
