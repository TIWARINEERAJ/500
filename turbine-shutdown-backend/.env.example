# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=turbine_shutdown
DB_USER=postgres
DB_PASSWORD=postgres

# Authentication
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=1h
PASSWORD_SALT_ROUNDS=10

# OPC UA Configuration
OPCUA_ENDPOINT=opc.tcp://localhost:4840

# Logging
LOG_LEVEL=info
